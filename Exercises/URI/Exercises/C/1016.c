#include <stdio.h>                              // Bilbioteca padrão de entrada e saída
// #include <stdlib.h>
 
int main() {                                    // Função main():
    int d;                                          // Declara a variável

    scanf("%d", &d);                                // Pede a entrada ao usuários
    printf("%d minutos\n", d*2);                    // Mostra a saída
    
    // system("pause");
    return 0;
}