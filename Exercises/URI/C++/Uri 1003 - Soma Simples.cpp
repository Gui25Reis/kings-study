#include <iostream>                                     // Biblioteca padrão entrada/saída

using namespace std;

int main() {                                            // Função main:
    int A, B;                                               // Declara as variáveis

    cin >> A;                                               // Pede o 1º número
    cin >> B;                                               // Pede o 2º número

    cout << "SOMA = " << A+B << endl;                       // Mostra a soma deles
    // system("pause");
	return 0;
}