#include <stdio.h>              // Bilbioteca padrão de entrada e saída
// #include <stdlib.h>
 
int main() {                    // Função main():
    int a, b;                       // Declara as variáveis inteiras

    scanf("%d",&a);                 // 1ª Entrada
    scanf("%d",&b);                 // 2ª Entrada
    printf("X = %d\n", a+b);        // Saída

    // system("pause");
    return 0;
}