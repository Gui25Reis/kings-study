#include <stdio.h>              // Bilbioteca padrão de entrada e saída
// #include <stdlib.h>
 
int main() {                    // Função main():
    int A, B;                       // Cria as variáveis

    scanf("%d", &A);                // Pede o 1º número
    scanf("%d", &B);                // Pede o 2º número

    printf("SOMA = %d\n", A+B);     // Mostra a soma deles
    // system("pause");             
    return 0;       
}